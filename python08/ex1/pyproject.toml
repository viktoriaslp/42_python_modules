[tool.poetry]
package-mode = false
name = "matrix-project"
version = "0.1.0"
description = "Data analysis in the Matrix"
authors = ["vslyunko"]

[tool.poetry.dependencies]
python = "^3.10"
pandas = "*"
numpy = "*"
matplotlib = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"